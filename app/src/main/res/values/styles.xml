<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>-->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:windowBackground">@android:color/white</item> <!-- cor de background do corpo do app -->
        <item name="android:itemBackground">@android:color/white</item><!-- cor de background do item de menu -->
        <item name="android:textColor">@android:color/black</item><!-- cor de background do texto do menu -->


    </style>

   <style name="botaoPadrao" parent="Theme.AppCompat.Light">
       <item name="colorButtonNormal">@color/colorPrimaryDarker</item>
       <item name="colorControlHighlight">@color/colorPrimary</item> <!-- mudar de cor ao clicar -->
       <item name="android:textColor">@android:color/white</item>
   </style>


</resources>
